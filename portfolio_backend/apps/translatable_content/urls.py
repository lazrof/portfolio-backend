from django.urls import path
#from .views import (ServicesList, SubscriptorsCreate)

app_name = 'apps.translatable_content'
""" urlpatterns = [
    path('services/', ServicesList.as_view(), name='service_list'),
    path('subscriptors/create', SubscriptorsCreate.as_view(), name='subscriptors_create'),
] """